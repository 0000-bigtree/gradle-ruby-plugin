group = 'bigtree'
version = '0.1.0'

apply plugin: 'maven'
apply plugin: 'maven-publish'
apply plugin: 'groovy'

archivesBaseName = 'ruby-plugin'

repositories {
  mavenLocal()
  mavenCentral()
}

dependencies {
  compile gradleApi()
  // groovy localGroovy()
  testCompile group: 'junit', name: 'junit', version: '4.10'
}
